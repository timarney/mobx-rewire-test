{
  "name": "react-app-rewire-mobx-test",
  "version": "0.5.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "canary"
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-app-rewire-mobx": "canary",
    "react-app-rewired": "canary",
    "react-dom": "^15.4.2"
  },
  "scripts": {
    "start": "react-app-rewired start",
    "build": "react-app-rewired build",
    "test": "react-app-rewired test --env=jsdom"
  }
}
